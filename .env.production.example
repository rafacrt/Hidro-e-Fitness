# Banco de dados
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hidrofitness

# Hasura
HASURA_ADMIN_SECRET=troque_este_segredo
# JWT do Hasura: objeto JSON com type e key
HASURA_GRAPHQL_JWT_SECRET={"type":"HS256","key":"troque_este_segredo_jwt"}

# App (Next.js)
# Deve ser o mesmo segredo (chave) usado no HASURA_GRAPHQL_JWT_SECRET.key
JWT_SECRET=troque_este_segredo_jwt