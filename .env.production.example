# Banco de dados
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hidrofitness

# Hasura
HASURA_ADMIN_SECRET=troque_este_segredo

# App (Next.js) e Hasura JWT
# IMPORTANTE: Use APENAS esta variável. O docker-compose interpolará automaticamente no formato JSON do Hasura
# O valor deve ser uma string longa e aleatória (mínimo 32 caracteres)
JWT_SECRET=troque_este_segredo_jwt_com_minimo_32_caracteres